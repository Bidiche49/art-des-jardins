# IMP-025: Ajouter BreadcrumbList Schema.org vitrine

**Type:** Improvement
**Statut:** Fait
**Priorite:** Basse
**Complexite:** XS
**Tags:** vitrine, seo
**Date creation:** 2026-02-12

---

## Description

Les pages services et villes ont un breadcrumb HTML visible mais pas de schema BreadcrumbList en JSON-LD. Google ne peut pas generer de rich snippets breadcrumb dans les resultats de recherche.

## Criteres d'acceptation

- [ ] Composant BreadcrumbSchema cree
- [ ] Utilise sur les pages services/[slug] et [serviceCity]
- [ ] Schema valide sur validator.schema.org

## Fichiers concernes

- `apps/vitrine/src/components/seo/BreadcrumbSchema.tsx` - CREER
- `apps/vitrine/src/app/services/[slug]/page.tsx:64-74`
- `apps/vitrine/src/app/[serviceCity]/page.tsx`

## Analyse / Approche

```typescript
export function BreadcrumbSchema({ items }: { items: { name: string; url: string }[] }) {
  const schema = {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: items.map((item, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: item.name,
      item: item.url,
    })),
  };
  return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />;
}
```

## Tests de validation

- [ ] JSON-LD BreadcrumbList present dans le HTML des pages concernees
- [ ] Valide sur https://validator.schema.org/
