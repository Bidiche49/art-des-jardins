# ===========================================
# ENVIRONMENT: TEST
# ===========================================

NODE_ENV=test

# Database (separate test database)
POSTGRES_USER=artjardin_test
POSTGRES_PASSWORD=artjardin_test
POSTGRES_DB=artjardin_test
DATABASE_URL=postgresql://artjardin_test:artjardin_test@localhost:5433/artjardin_test

# API
API_PORT=3001
API_HOST=127.0.0.1

# JWT (test secret)
JWT_SECRET=test-secret-key-for-automated-tests-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# S3 Storage (mocked in tests)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=testaccess
S3_SECRET_KEY=testsecret
S3_BUCKET=art-et-jardin-test
S3_REGION=us-east-1

# Email (disabled in tests)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=test@artjardin.local

# URLs
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3002
VITE_API_URL=http://localhost:3001

# Logging (minimal in tests)
LOG_LEVEL=error
